# Models

This directory is intended for model weights and related artifacts.

- `pretrained/` for externally obtained base models
- `trained/` for weights produced by your own training runs

Large binary weights are not committed by default.
